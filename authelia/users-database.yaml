apiVersion: v1
kind: Secret
metadata:
  name: users-database
  namespace: production
type: Opaque
stringData:
  users-database.yaml: |
    users:
      imranr:
        disabled: false
        displayname: "Imran"
        password: "***REMOVED***"  # yamllint disable-line rule:line-length
        email: contact@example.org
        groups:
          - admins
          - dev
      # PROD TODO: Replace this with the prod version