apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: plausible
  namespace: production
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`plausible.staging.example.org`)
      kind: Rule
      services:
        - name: plausible
          port: 8000
  tls:
    secretName: example-org-tls