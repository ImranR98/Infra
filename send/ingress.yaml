apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: send
  namespace: production
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`send.staging.example.org`)
      kind: Rule      
      services:
        - name: send
          port: 3000
  tls:
    secretName: example-org-tls
