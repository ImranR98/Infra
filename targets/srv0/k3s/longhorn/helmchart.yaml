apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: longhorn-install
  namespace: kube-system
spec:
  chart: longhorn
  repo: https://charts.longhorn.io
  failurePolicy: abort
  targetNamespace: longhorn-system
  createNamespace: true
  version: 1.12.0
  valuesContent: |-
    # Single replica is intentional for single-node cluster.
    # Increase defaultClassReplicaCount + defaultReplicaCount when adding additional nodes.
    persistence:
      defaultClassReplicaCount: 1
      reclaimPolicy: "Retain"
    defaultSettings:
      defaultReplicaCount: "1"
      defaultDataPath: "/var/lib/rancher/longhorn"
      storageMinimalAvailablePercentage: 5
      # 1000% overprovisioning on single-node clusters avoids false "disk full"
      # errors since there's no way to add more storage without disrupting workloads.
      storageOverProvisioningPercentage: 1000
      replicaSoftAntiAffinity: true
    resources:
      limits:
        memory: 2Gi
      requests:
        memory: 128Mi
    longhorn:
      manager:
        resources:
          limits:
            memory: 2Gi
          requests:
            memory: 128Mi
      ui:
        resources:
          limits:
            memory: 512Mi
          requests:
            memory: 128Mi
    csi:
      attacher:
        resources:
          limits:
            memory: 512Mi
          requests:
            memory: 128Mi
      provisioner:
        resources:
          limits:
            memory: 512Mi
          requests:
            memory: 128Mi
      resizer:
        resources:
          limits:
            memory: 512Mi
          requests:
            memory: 128Mi
      snapshotter:
        resources:
          limits:
            memory: 512Mi
          requests:
            memory: 128Mi
