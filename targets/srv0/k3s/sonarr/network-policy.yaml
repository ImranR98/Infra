---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-sonarr-ingress
  namespace: apps
spec:
  podSelector:
    matchLabels:
      app: sonarr
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: prowlarr
        - podSelector:
            matchLabels:
              app.kubernetes.io/instance: seerr
      ports:
        - port: 8989
  policyTypes:
    - Ingress
