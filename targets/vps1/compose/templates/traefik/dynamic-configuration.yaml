http:
  routers:
    http-to-frps:
      rule: "Host(`pc0.$SERVICES_DOMAIN`) || Host(`*.pc0.$SERVICES_DOMAIN`)"
      entryPoints: [web]
      service: http-frps
      priority: 100
  services:
    http-frps:
      loadBalancer:
        servers:
          - url: "http://frps:8080"

tcp:
  serversTransports:
    frps-proxy:
      proxyProtocol:
        version: 2
  routers:
    https-to-frps:
      rule: "HostSNI(`pc0.$SERVICES_DOMAIN`) || HostSNI(`*.pc0.$SERVICES_DOMAIN`)"
      entryPoints: [websecure]
      service: https-frps
      tls:
        passthrough: true
  services:
    https-frps:
      loadBalancer:
        serversTransport: frps-proxy
        servers:
          - address: "frps:8443"
